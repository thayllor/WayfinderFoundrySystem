<div class="effect-activation">
  <h3 style="color: #2d5016; margin: 10px 0;">{{title}}</h3>
  <p>{{{chatEffect}}}</p>
  {{#if requiresRoll}}
    <p style="font-weight: bold; color: #1e4d6b;">Roll requerido:
      {{rollFormula}}</p>
  {{/if}}
</div>